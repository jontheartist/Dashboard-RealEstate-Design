import React from 'react'
import styled from 'styled-components'


const Title = styled.h1`
font-weight: 500;
color: ${({ theme }) => theme.textColor};
font - size: 1.3rem;
`
const DepositsCount = styled.h1`
    `





const Deposits = (title, data, count) => {
    return (
        <div>

        </div>
    )
}

export default Deposits
